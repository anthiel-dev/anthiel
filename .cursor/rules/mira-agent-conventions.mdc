---
description: Monorepo-wide tooling, code style, and agent behavior for the Anthiel project
alwaysApply: true
---

# Anthiel agent conventions

## Tooling

- Package manager: **Bun** (`bun install`, `bun run <script>`, `bunx`). See `use-bun-instead-of-node-vite-npm-pnpm.mdc` when editing package manifests.
- Lint: `bun run lint` (oxlint). Format: `bun run format` (oxfmt).
- Typecheck: `bun run typecheck` from root or per app.
- Project map and commands: **`AGENTS.md`** at repo root.

## Code changes

- Minimize scope — smallest correct diff; do not change unrelated code.
- Match existing conventions in the file and feature you touch.
- Named exports; function declarations for components (`function Foo()`).
- Use `type` for type-only exports.
- Comments only for non-obvious business logic — not on every line.

## Git

- **Do not commit** unless the user explicitly asks.
- **Do not push** unless the user explicitly asks.
- Follow git safety: no force push to main, no `--no-verify`, no amending pushed commits.

## App-specific patterns

Load automatically via glob rules — do not guess:

- `apps/dashboard` → `mira-frontend-feature-patterns.mdc`, `mira-frontend-api-integration.mdc`
- `apps/landing` → `mira-frontend-feature-patterns.mdc` when applicable
- `apps/backend` → `backend-modular-structure.mdc`, `mira-backend-routes-services.mdc`

Skills in `.cursor/skills/` for deep task workflows (task-boards, backend modular, transitions).

## Golden paths (quick)

| Area | First step |
|------|------------|
| Dashboard feature | `@apps/dashboard/src/features/projects/` or `invoices/` + `mira-frontend-feature-patterns.mdc` |
| Backend module | `@apps/backend/src/modules/projects/` or `invoices/` + `backend-modular-structure.mdc` |
| Landing | `@apps/landing/` |
| Tracked work | Markdown task-board `tasks-management/<epic>/` — see `anthiel-task-boards.mdc` + skill `anthiel-task-boards` |
| Stale `.cursor/plans/` | Read banner — treat as historical; trust `AGENTS.md` and live code |
