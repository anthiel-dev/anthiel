# Baked into the client at `bun run build` — must be reachable from the browser.
# Local / on-VPS with Caddy + /etc/hosts:
VITE_BETTER_AUTH_URL=http://api.anthiel.local

# Production behind Cloudflare (use this on the VPS for real users):
# VITE_BETTER_AUTH_URL=https://api.yourdomain.com
